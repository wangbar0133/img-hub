# IMG-Hub 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置

# ===================
# Docker Hub 部署配置（必填）
# ===================
# Docker Hub 用户名（替换为你的用户名）
DOCKER_USERNAME=wangbar01334
# 镜像标签版本
IMAGE_TAG=latest

# ===================
# 数据库配置
# ===================
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=admin123
MONGO_DATABASE=img-hub

# ===================
# 后端配置
# ===================
RUST_LOG=info
BACKEND_PORT=8000

# ===================
# 前端配置
# ===================
NODE_ENV=production
# 容器内部通信URL（后端容器名）
BACKEND_URL=http://img-hub-backend:8000
# 浏览器访问的URL（外部访问地址）
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# ===================
# Nginx配置
# ===================
HTTP_PORT=80
HTTPS_PORT=443

# ===================
# 生产环境配置（必填）
# ===================
# 域名配置（用于HTTPS证书）
DOMAIN_NAME=img.neicun.online
# SSL证书申请邮箱
SSL_EMAIL=your-email@example.com

# 使用域名时的前端URL配置（启用HTTPS后使用）
NEXT_PUBLIC_BACKEND_URL=https://img.neicun.online

# ===================
# SSL配置（自动生成）
# ===================
# 证书路径由 Let's Encrypt 自动管理
# /etc/letsencrypt/live/yourdomain.com/fullchain.pem
# /etc/letsencrypt/live/yourdomain.com/privkey.pem